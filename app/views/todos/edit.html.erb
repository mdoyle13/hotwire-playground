<turbo-frame id="<%= dom_id(@todo) %>">
  <%= form_with(model: @todo) do |form| %>
  
    <div class="field">
      <%= form.label :content %>
      <%= form.text_field :content %>
    </div>
  
  <% end %>
</turbo-frame>
