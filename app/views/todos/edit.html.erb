<%= turbo_frame_tag @todo do %>
  <%= form_with(model: @todo) do |form| %>
  
    <div class="field">
      <%= form.text_field :content %>
      <%= form.submit %>
    </div>
  
  <% end %>
<% end %>
